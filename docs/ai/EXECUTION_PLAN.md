# Execution Plan

> Generated by Coded by M — Site Architecture System. Source of truth: `/docs/ai/DISCOVERY.md` and `/docs/ai/DECISIONS_LOG.md`.

## Implementation status (2026-06-09)
All phases 01–07 implemented and verified (clean production build, all routes 200/404, no runtime errors). Next.js 16 + React 19 + Tailwind v4 + Lenis + Motion. Remaining work is **content population** (replace placeholders per `CONTENT_PENDING.md`) and brand-identity tokens once defined.
- [x] 01 Foundation · [x] 02 Components · [x] 03 Pages · [x] 04 Visual · [x] 05 Motion · [x] 06 Responsive · [x] 07 Quality

## Required Reading Order
1. `/CLAUDE.md`
2. `/docs/ai/MASTER_CONTEXT.md`
3. `/docs/ai/PROJECT.md`
4. `/docs/ai/SITE_ARCHITECTURE.md`
5. `/docs/ai/PAGE_BLUEPRINTS.md`
6. `/docs/ai/DESIGN_SYSTEM.md`
7. `/docs/ai/MOTION_DIRECTION.md`
8. `/docs/ai/QA_CHECKLIST.md`

## Execution Prompt (reuse each phase)
Read all required docs. Diagnose before editing. Implement only the current phase. Use placeholders for missing client content. Report changed files, decisions, risks and pending items.

## Phase 01 — Project foundation
### Goal
Scaffold Next.js (App Router) + TypeScript + Tailwind. Configure `next/font`, Lenis, motion lib (GSAP and/or Framer Motion). Establish design tokens (color, type, spacing, grid), global styles, base layout (header + footer-index), and the hairline grid system as reusable primitives.
### Deliverables
Working dev build, tokens wired to Tailwind config, Header/Footer shells, container/grid + Hairline components.

## Phase 02 — Essential components
Build the component library from `DESIGN_SYSTEM.md`: Navbar, Footer, Buttons/Links (hairline style), Section header, Project card, Technical sheet, Form fields, Type filter tags, image wrapper (ratio + `next/image`).

## Phase 03 — Page structure
Implement routes and static structure with placeholder content: `/`, `/projetos`, `/projetos/[slug]`, `/estudio`, `/contato`. Wire navigation and CTAs. Set up project content source (MDX/JSON) with 3–5 placeholder projects.

## Phase 04 — Visual direction pass
Apply full visual fidelity: typography scale, exposed grid/hairlines, whitespace rhythm, near-mono palette, image treatment. Ensure "no template feel" and editorial tension.

## Phase 05 — Motion and interactions
Implement approved motion from `MOTION_DIRECTION.md`: hairline draw-in, image mask reveals, scroll (Lenis), microinteractions, page transitions. Add `prefers-reduced-motion` fallbacks.

## Phase 06 — Responsive pass
Desktop → notebook → tablet → mobile. Verify grid collapse, hairline-as-divider behavior, image ratios, no overflow.

## Phase 07 — Quality pass
Run `QA_CHECKLIST.md`: SEO meta/H1, accessibility, performance (LCP/CLS/INP), console clean, placeholder audit.

## Completion Criteria
Use `/docs/ai/QA_CHECKLIST.md` as the project readiness gate. Replace placeholders as real content arrives (`CONTENT_PENDING.md`).
